//
//  ContentView.swift
//  Example App
//
//  Created by TOKIYA on 2023/04/02.
//

import SwiftUI

struct ContentView: View {
    // training for github
    // test
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("Hello, world!")
        }
        .padding()
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
